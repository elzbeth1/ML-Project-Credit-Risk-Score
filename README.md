# ML-Project-Credit-Risk-Score
Credit Risk Score Project to find defaulters 

The flow is:
User Input → prepare_df() → scaled + aligned features → 
calculate_credit_score() → probability + score + rating
